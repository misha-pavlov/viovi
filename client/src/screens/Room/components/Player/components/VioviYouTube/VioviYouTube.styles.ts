import styled from 'styled-components';
import { colors } from '../../../../../../config/colors';
import { DefaultText } from '../../../../../../config/fonts';

export const PleaseEnterLink = styled(DefaultText)`
  color: ${colors.white1};
`;
